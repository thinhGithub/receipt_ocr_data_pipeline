NHẬT KÝ VÀ TIẾN ĐỘ
Cập nhật: 31/08/2026

1. Dữ liệu
- Chạy: python scripts/inspect_dataset.py
- Đã sửa nhãn TOTAL_TOTAL_COST thành TOTAL_COST.
- Đã loại 2 ảnh không có annotation.
- Kết quả: 1.153 ảnh hợp lệ, lưu trong data/interim/mc_ocr2021/.

2. Ground truth
- Chạy: python scripts/build_ground_truth.py
- Bốn trường: seller, address, timestamp, total_cost.
- Tỷ lệ thiếu nhãn gốc: seller 7,11%; address 6,59%; timestamp 5,64%;
  total_cost 6,16%.

3. Pipeline đã hoàn thành
- OCR Tesseract pretrained vie+eng.
- Làm sạch OCR, trích xuất đủ 4 trường và xử lý address nhiều dòng.
- Chuẩn hóa timestamp, total_cost và kiểm tra dữ liệu hợp lệ.
- Pipeline end-to-end, cấu hình YAML và lưu JSONL tùy chọn.
- Cache OCR và chia cố định tập development/final.
- Đánh giá từng trường và macro evaluation.

4. Baseline development (40 ảnh, seed 42)
- Seller: coverage 94,87%; fuzzy similarity 44,95%.
- Address: coverage 97,22%; fuzzy similarity 52,31%.
- Timestamp: coverage 79,41%; exact match 67,65%.
- Total cost: coverage 53,85%; exact numeric match 35,90%.
- Macro: coverage 81,34%; exact match 30,43%.
- Cache: 40 hit, 0 miss.

Kết quả nằm tại reports/metrics/ocr_baseline/development/.

5. Chưa hoàn thành
- Preprocessing ảnh thực tế: grayscale, resize, threshold, denoise, deskew...
- Thí nghiệm so sánh ảnh gốc/preprocessed và các chế độ PSM.
- Cải thiện rule cho seller, address và total_cost.
- Chạy đánh giá chính thức trên tập final.
- Tạo notebook và báo cáo kết quả cuối cùng.

6. Lệnh thường dùng
python scripts/inspect_dataset.py
python scripts/build_ground_truth.py
python scripts/run_ocr_baseline.py --split development --sample-size 40 --seed 42
python scripts/run_ocr_baseline.py --split final --sample-size 0
python -m pytest -q
